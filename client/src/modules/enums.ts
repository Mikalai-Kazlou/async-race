export enum Constants {
  BaseURL = 'http://127.0.0.1:3000',
  NumberOfColors = 16777215,
}

export enum Pages {
  Garage = 'Garage',
  Winners = 'Winners',
}

export enum SortingColumns {
  Id = 'id',
  Wins = 'wins',
  Time = 'time',
}

export enum SortingOrder {
  Asc = 'ASC',
  Desc = 'DESC',
}

export enum CarActions {
  Create = 'create',
  Update = 'update',
  Remove = 'remove',
  Select = 'select',
  Start = 'start',
  Stop = 'stop',
}
